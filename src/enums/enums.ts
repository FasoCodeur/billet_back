export enum StatusPayment {
  PENDING ='pending',
  CONFIRMED ='confirmed',
  CANCELED ='canceled'
}

export enum StatusReservation {
  PENDING ='pending',
  CONFIRMED ='confirmed',
  CANCELED ='canceled'
}
export enum StatusSeat {
  PENDING = 'pending',
  DESACTIVER = 'Desactiver',
  VENDUE = 'Vendue'
}

// export enum Options {

// }